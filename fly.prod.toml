app = "integrity-prod"
primary_region = "syd"

[http_service]
  internal_port = 5000           # KEEP THIS (Matches your code)
  force_https = true
  auto_stop_machines = false     # CHANGED: Prevents sleeping
  auto_start_machines = true
  min_machines_running = 1       # CHANGED: Forces 1 machine to always run
  processes = ["app"]